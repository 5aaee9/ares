# Seam-cache bounded parity plan

1. Read wave5 handoff and source-owned ARD; identify original inputs, actual
   AppImage and published base. Capture fresh anchor/two-walls/wipe-off/
   solid-rectilinear CLI outputs and strict red comparisons before production
   edits. Use exclusively owned targets and evidence directories.
2. Derive processor replay input explicitly from actual reference output
   (remove generated progress, restore first placeholder), leaving all motion
   bytes unchanged. Compare full process output at the existing byte seam.
   Port only process_G1 seam lifecycle/cache association; retain total time.
3. Validate complete processor replay and project output before/after, including
   separately identified logical-cursor integration where needed. Retain all
   unrelated raw output differences; never credit a bounded pass as parity.
4. Run focused/full core nextest, fmt, clippy, WASM compilation and physical LOC
   checks. Preserve logs/exits/hashes/binary patch. Commit only a verified
   bounded correction with a clean index to local coord/parity-m73-w6; otherwise
   return partial with explicit required red cases and no candidate.

Included/deferred ownership and non-negotiable evidence policy are in
`docs/architecture/processor-seam-cache-association.md`. No routing, cursor,
fill, scheduler, rounding, E-state or option-harness edits are authorized.

## Bounded validation outcome

- Fresh public CLI on the published base: all four strict comparisons red;
  full original and changed residuals retained. No producer pass is claimed.
- Processor byte replay: anchor/two-walls pass before; wipe-off, rectilinear
  and custom-start classification fail before and pass after. Complete bytes
  retained, with unchanged actual-Orca total/first-layer duration fields.
- Separately archived logical-cursor integration: anchor/two-walls pass before
  and after; wipe-off/rectilinear strictly red before and green after. No
  disqualified rectilinear instrument values enter expectations.
- Final core nextest: 6796 passed, one pre-existing ignored test; processor
  output isolation CLI tests: four passed. fmt, clippy and WASM core/adapter
  check exit zero (existing warnings retained). Changed Rust sources <400 LOC.
- Supervisor approved committed processor regressions/project data plus
  repeatable external CLI integration evidence, rather than an ignored or
  permanently red committed CLI test. Published-source rectilinear remains
  unchanged because unrelated missing logical-cursor commands shift lookup.
- Exact formatted duration preservation is observed; no new bilateral raw
  double-time instrumentation is claimed. Scheduler/arithmetic are unchanged.
- Reproduction script and exact fixture/patch identities are committed under
  `tests/processor_seam_cache`; full run evidence is in the wave6 evidence root.

## Pre-publication fixture/lint follow-up

Keep the same included/deferred source boundary. Add only fixture-local Git
attributes to preserve verbatim G-code checkout bytes and exempt the captured
trailing whitespace from diagnostics, without hiding ordinary diffs. Flatten
cache-association control flow using the existing invariant that linear motion
produces at most one block; do not change detector, timing or counter semantics.

Verify all six G-code files against the original manifest, committed/staged
blobs and retained captures. Rerun the 33 existing processor tests (including
five byte goldens), compare before/after output exactly, verify fmt/clippy and
absence of the two new nesting warnings, and repeat the approved external
integration script. Preserve the old commit/evidence and fast-forward the local
candidate with one follow-up commit only after these checks pass.
